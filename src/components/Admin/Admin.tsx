

import React from 'react'

    class Admin extends React.Component {  // Should be a stateless functional component
    render() {
        return (
            <div>
                <p>This is the Admin component!</p>
            </div>
        )
    }
}

export default Admin
